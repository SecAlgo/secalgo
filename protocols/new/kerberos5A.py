# -*- generated by 1.0.12 -*-
import da
PatternExpr_265 = da.pat.TuplePattern([da.pat.ConstantPattern('krb_as_req'), da.pat.FreePattern('i'), da.pat.FreePattern('pvno'), da.pat.FreePattern('msg_type'), da.pat.FreePattern('padata'), da.pat.FreePattern('req_body')])
PatternExpr_280 = da.pat.FreePattern('C')
PatternExpr_528 = da.pat.TuplePattern([da.pat.ConstantPattern('krb_tgs_req'), da.pat.FreePattern('i'), da.pat.ConstantPattern(5), da.pat.ConstantPattern(12), da.pat.FreePattern('padata'), da.pat.FreePattern('req_body')])
PatternExpr_543 = da.pat.FreePattern('C')
PatternExpr_877 = da.pat.TuplePattern([da.pat.ConstantPattern('krb_ap_req'), da.pat.FreePattern('i'), da.pat.ConstantPattern(5), da.pat.ConstantPattern(14), da.pat.BoundPattern('_BoundPattern891_'), da.pat.FreePattern('ticket'), da.pat.FreePattern('authenticator')])
PatternExpr_898 = da.pat.FreePattern('C')
PatternExpr_1097 = da.pat.TuplePattern([da.pat.ConstantPattern('krb_as_rep'), da.pat.BoundPattern('_BoundPattern1100_'), da.pat.ConstantPattern(5), da.pat.ConstantPattern(11), da.pat.FreePattern('padata'), da.pat.ConstantPattern('TEST'), da.pat.BoundPattern('_BoundPattern1109_'), da.pat.FreePattern('tgt'), da.pat.FreePattern('enc_part')])
PatternExpr_1116 = da.pat.FreePattern('AS')
PatternExpr_1275 = da.pat.TuplePattern([da.pat.ConstantPattern('krb_tgs_rep'), da.pat.BoundPattern('_BoundPattern1278_'), da.pat.ConstantPattern(5), da.pat.ConstantPattern(13), da.pat.FreePattern('tgs_padata'), da.pat.ConstantPattern('TEST'), da.pat.BoundPattern('_BoundPattern1287_'), da.pat.FreePattern('ticket'), da.pat.FreePattern('tgs_enc_part')])
PatternExpr_1294 = da.pat.FreePattern('TGS')
_config_object = {}
import sys, time, random
from sa.secalgoB import *
DEFAULT_TGT_DURATION = 36000
DEFAULT_TKT_DURATION = 3600
DEFAULT_SKEW = 300
KRB_AP_REQ_TYPE = 14

class Role_AuthServer(da.DistProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._events.extend([da.pat.EventPattern(da.pat.ReceivedEvent, '_Role_AuthServerReceivedEvent_0', PatternExpr_265, sources=[PatternExpr_280], destinations=None, timestamps=None, record_history=None, handlers=[self._Role_AuthServer_handler_264])])

    def setup(self, clients, servers, TGS, client_keys, server_keys, key_TGS, **rest_1581):
        super().setup(clients=clients, servers=servers, TGS=TGS, client_keys=client_keys, server_keys=server_keys, key_TGS=key_TGS, **rest_1581)
        self._state.clients = clients
        self._state.servers = servers
        self._state.TGS = TGS
        self._state.client_keys = client_keys
        self._state.server_keys = server_keys
        self._state.key_TGS = key_TGS
        self._state.terminate = False
        self._state.supported_etypes = {17, 18}
        self._state.max_tgt_duration = DEFAULT_TGT_DURATION
        self._state.skew = DEFAULT_SKEW
        self._state.per_client_lifetimes = dict()
        for c in self._state.clients:
            self._state.per_client_lifetimes[c] = DEFAULT_TGT_DURATION
        self._state.per_server_lifetimes = dict()
        for s in self._state.servers:
            self._state.per_server_lifetimes[s] = DEFAULT_TGT_DURATION

    @dec_proto_run_timer
    def run(self):
        self._state.terminate = False
        super()._label('_st_label_260', block=False)
        _st_label_260 = 0
        while (_st_label_260 == 0):
            _st_label_260 += 1
            if self._state.terminate:
                _st_label_260 += 1
            else:
                super()._label('_st_label_260', block=True)
                _st_label_260 -= 1

    def _Role_AuthServer_handler_264(self, i, pvno, msg_type, padata, req_body, C):
        (kdc_options, cname, realm, sname, krb_from, till, rtime, nonce, etype, addresses, enc_authorization_data, additional_tickets) = req_body
        if (not (cname in self._state.clients)):
            return
        if (not (sname in self._state.servers)):
            return
        chosen_etype = None
        for et in etype:
            if ((et in self._state.supported_etypes) and (chosen_etype == None)):
                chosen_etype = et
        if (chosen_etype == None):
            return
        key_C_TGS = keygen('shared')
        if ((krb_from == None) or (krb_from < (time.time() + self._state.skew))):
            tgt_start = time.time()
        else:
            return
        tgt_end = min(till, (tgt_start + self._state.per_client_lifetimes[cname]), (tgt_start + self._state.per_server_lifetimes[sname]), (tgt_start + self._state.max_tgt_duration))
        tgt_enc_part = (dict(), key_C_TGS, 'TEST', cname, [], tgt_start, tgt_start, tgt_end, None, [], [])
        new_tgt = [5, 'TEST', sname, encrypt(tgt_enc_part, key=self._state.key_TGS)]
        as_rep_enc_part = [key_C_TGS, (0, 0), nonce, 0, dict(), tgt_start, tgt_start, tgt_end, 0, 'TEST', sname, []]
        self.output('AS - Sent TGT to client,', cname)
        self.send(('krb_as_rep', i, 5, 11, [], 'TEST', cname, new_tgt, encrypt(as_rep_enc_part, key=self._state.client_keys[cname])), to=C)
        self._state.terminate = True
    _Role_AuthServer_handler_264._labels = None
    _Role_AuthServer_handler_264._notlabels = None

class Role_TGS(da.DistProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._events.extend([da.pat.EventPattern(da.pat.ReceivedEvent, '_Role_TGSReceivedEvent_0', PatternExpr_528, sources=[PatternExpr_543], destinations=None, timestamps=None, record_history=None, handlers=[self._Role_TGS_handler_527])])

    def setup(self, servers, AS, key_AS_TGS, server_keys, **rest_1581):
        super().setup(servers=servers, AS=AS, key_AS_TGS=key_AS_TGS, server_keys=server_keys, **rest_1581)
        self._state.servers = servers
        self._state.AS = AS
        self._state.key_AS_TGS = key_AS_TGS
        self._state.server_keys = server_keys
        self._state.terminate = False
        self._state.supported_etypes = {17, 18}
        self._state.skew = DEFAULT_SKEW
        self._state.max_tkt_duration = DEFAULT_TKT_DURATION
        self._state.per_server_lifetimes = dict()
        for s in self._state.server_keys:
            self._state.per_server_lifetimes[s] = DEFAULT_TKT_DURATION

    @dec_proto_run_timer
    def run(self):
        self._state.terminate = False
        super()._label('_st_label_523', block=False)
        _st_label_523 = 0
        while (_st_label_523 == 0):
            _st_label_523 += 1
            if self._state.terminate:
                _st_label_523 += 1
            else:
                super()._label('_st_label_523', block=True)
                _st_label_523 -= 1

    def _Role_TGS_handler_527(self, i, padata, req_body, C):
        (_, msg_type, ap_options, tgt, authenticator) = padata[0]
        if (not (msg_type == 14)):
            return
        (_, realm, sname, tgt_enc_part) = tgt
        (tgt_flags, key_C_TGS, tgt_crealm, tgt_cname, tgt_transited, tgt_authtime, tgt_starttime, tgt_endtime, tgt_renew_till, tgt_caddr, tgt_authorization_data) = decrypt(tgt_enc_part, key=self._state.key_AS_TGS)
        (_, au_crealm, au_cname, cksum, cusec, ctime, subkey, seq_number, authorization_data) = decrypt(authenticator, key=key_C_TGS)
        if ((not (tgt_cname == au_cname)) or (not (tgt_crealm == au_crealm))):
            return
        local_time = time.time()
        if (abs((local_time - ctime)) > self._state.skew):
            return
        if (tgt_starttime > (local_time + self._state.skew)):
            return
        if ((local_time - tgt_endtime) > self._state.skew):
            return
        self.output('TGS - Authenticated client:', au_cname)
        (kdc_options, cname, realm, sname, krb_from, till, rtime, nonce, etype, addresses, enc_authorization_data, additional_tickets) = req_body
        if (not (sname in self._state.server_keys)):
            return
        chosen_etype = None
        for et in etype:
            if ((et in self._state.supported_etypes) and (chosen_etype == None)):
                chosen_etype = et
        if (chosen_etype == None):
            return
        key_C_S = keygen('shared')
        if ((krb_from == None) or (krb_from < (local_time + self._state.skew))):
            ticket_start = local_time
        ticket_end = min(till, tgt_endtime, (tgt_starttime + self._state.per_server_lifetimes[sname]), (tgt_starttime + self._state.max_tkt_duration))
        ticket_enc_part = [dict(), key_C_S, tgt_crealm, tgt_cname, tgt_transited, tgt_authtime, ticket_start, ticket_end, 0, tgt_caddr, []]
        new_ticket = [5, 'TEST', sname, encrypt(ticket_enc_part, key=self._state.server_keys[sname])]
        tgs_rep_enc_part = [key_C_S, (0, 0), nonce, 0, dict(), tgt_authtime, ticket_start, ticket_end, 0, 'TEST', sname, tgt_caddr]
        self.output('TGS - Sent Ticket for server', sname, 'to client,', C)
        self.send(('krb_tgs_rep', i, 5, 13, [], 'TEST', tgt_cname, new_ticket, encrypt(tgs_rep_enc_part, key=key_C_TGS)), to=C)
        self._state.terminate = True
    _Role_TGS_handler_527._labels = None
    _Role_TGS_handler_527._notlabels = None

class Role_AppServer(da.DistProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._events.extend([da.pat.EventPattern(da.pat.ReceivedEvent, '_Role_AppServerReceivedEvent_0', PatternExpr_877, sources=[PatternExpr_898], destinations=None, timestamps=None, record_history=None, handlers=[self._Role_AppServer_handler_876])])

    def setup(self, AS, TGS, key_S_AS, key_S_TGS, **rest_1581):
        super().setup(AS=AS, TGS=TGS, key_S_AS=key_S_AS, key_S_TGS=key_S_TGS, **rest_1581)
        self._state.AS = AS
        self._state.TGS = TGS
        self._state.key_S_AS = key_S_AS
        self._state.key_S_TGS = key_S_TGS
        self._state.terminate = False
        self._state.S = self._id
        self._state.skew = DEFAULT_SKEW

    @dec_proto_run_timer
    def run(self):
        self._state.terminate = False
        super()._label('_st_label_872', block=False)
        _st_label_872 = 0
        while (_st_label_872 == 0):
            _st_label_872 += 1
            if self._state.terminate:
                _st_label_872 += 1
            else:
                super()._label('_st_label_872', block=True)
                _st_label_872 -= 1

    def _Role_AppServer_handler_876(self, i, ticket, authenticator, C):
        (tkt_vno, realm, sname, tkt_enc_part) = ticket
        (tkt_flags, key_C_S, tkt_crealm, tkt_cname, tkt_transited, tkt_authtime, tkt_starttime, tkt_endtime, tkt_renew_till, tkt_caddr, tkt_authorization_data) = decrypt(tkt_enc_part, key=self._state.key_S_TGS)
        (_, au_crealm, au_cname, au_cksum, au_cusec, au_ctime, au_subkey, au_seq_number, au_authorization_data) = decrypt(authenticator, key=key_C_S)
        if ((not (au_crealm == tkt_crealm)) or (not (au_cname == tkt_cname))):
            return
        local_time = time.time()
        if (abs((local_time - au_ctime)) > self._state.skew):
            return
        if (tkt_starttime > (local_time + self._state.skew)):
            return
        if ((local_time - tkt_endtime) > self._state.skew):
            return
        self.output('Server:', self._state.S, '- Authenticated client:', au_cname)
        self._state.terminate = True
    _Role_AppServer_handler_876._labels = None
    _Role_AppServer_handler_876._notlabels = None

class Role_C(da.DistProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._Role_CReceivedEvent_0 = []
        self._Role_CReceivedEvent_1 = []
        self._events.extend([da.pat.EventPattern(da.pat.ReceivedEvent, '_Role_CReceivedEvent_0', PatternExpr_1097, sources=[PatternExpr_1116], destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_Role_CReceivedEvent_1', PatternExpr_1275, sources=[PatternExpr_1294], destinations=None, timestamps=None, record_history=True, handlers=[])])

    def setup(self, realm, servers, AS, TGS, key_C_AS, **rest_1581):
        super().setup(realm=realm, servers=servers, AS=AS, TGS=TGS, key_C_AS=key_C_AS, **rest_1581)
        self._state.realm = realm
        self._state.servers = servers
        self._state.AS = AS
        self._state.TGS = TGS
        self._state.key_C_AS = key_C_AS
        self._state.C = self._id
        self._state.i = 1

    @dec_proto_run_timer
    def run(self):
        S = random.choice(tuple(self._state.servers))
        nonce_C_AS = nonce()
        as_req_body = [dict(), self._state.C, 'TEST', S, 0, (time.time() + (5 * 3600)), 0, nonce_C_AS, [18, 17], [], [], []]
        self.output('Client - Sent request to AS for credentials for server,', S)
        self.send(('krb_as_req', self._state.i, 5, 10, [], as_req_body), to=self._state.AS)
        super()._label('_st_label_1094', block=False)
        padata = enc_part = tgt = AS = None

        def ExistentialOpExpr_1095():
            nonlocal padata, enc_part, tgt, AS
            for (_, (_, _, self._state.AS), (_ConstantPattern1126_, _BoundPattern1128_, _ConstantPattern1129_, _ConstantPattern1131_, padata, _ConstantPattern1134_, _BoundPattern1136_, tgt, enc_part)) in self._Role_CReceivedEvent_0:
                if (_ConstantPattern1126_ == 'krb_as_rep'):
                    if (_BoundPattern1128_ == self._state.i):
                        if (_ConstantPattern1129_ == 5):
                            if (_ConstantPattern1131_ == 11):
                                if (_ConstantPattern1134_ == 'TEST'):
                                    if (_BoundPattern1136_ == self._state.C):
                                        if True:
                                            return True
            return False
        _st_label_1094 = 0
        while (_st_label_1094 == 0):
            _st_label_1094 += 1
            if ExistentialOpExpr_1095():
                _st_label_1094 += 1
            else:
                super()._label('_st_label_1094', block=True)
                _st_label_1094 -= 1
        (key_C_TGS, last_req, as_nonce, key_expiration, flags, authtime, starttime, endtime, renew_till, srealm, sname, caddr) = decrypt(enc_part, key=self._state.key_C_AS)
        if (as_nonce == nonce_C_AS):
            if ((sname == S) and (srealm == 'TEST')):
                self.output('Client - Acquired TGT from AS')
        else:
            key_C_TGS = None
            tgt = None
        if (not (tgt == None)):
            authenticator = [5, self._state.realm, self._state.C, None, 0, time.time(), None, None, []]
            tgs_krb_ap_req = [5, 14, dict(), tgt, encrypt(authenticator, key=key_C_TGS)]
            nonce_C_TGS = nonce()
            tgs_req_body = [dict(), None, 'TEST', S, 0, (time.time() + (5 * 3600)), 0, nonce_C_TGS, [18, 17], [], [], []]
            self.output('Client - Sent TGT and request to TGS for credentials', 'for server,', S)
            self.send(('krb_tgs_req', self._state.i, 5, 12, [tgs_krb_ap_req], tgs_req_body), to=self._state.TGS)
            super()._label('_st_label_1272', block=False)
            tgs_enc_part = TGS = ticket = tgs_padata = None

            def ExistentialOpExpr_1273():
                nonlocal tgs_enc_part, TGS, ticket, tgs_padata
                for (_, (_, _, self._state.TGS), (_ConstantPattern1304_, _BoundPattern1306_, _ConstantPattern1307_, _ConstantPattern1309_, tgs_padata, _ConstantPattern1312_, _BoundPattern1314_, ticket, tgs_enc_part)) in self._Role_CReceivedEvent_1:
                    if (_ConstantPattern1304_ == 'krb_tgs_rep'):
                        if (_BoundPattern1306_ == self._state.i):
                            if (_ConstantPattern1307_ == 5):
                                if (_ConstantPattern1309_ == 13):
                                    if (_ConstantPattern1312_ == 'TEST'):
                                        if (_BoundPattern1314_ == self._state.C):
                                            if True:
                                                return True
                return False
            _st_label_1272 = 0
            while (_st_label_1272 == 0):
                _st_label_1272 += 1
                if ExistentialOpExpr_1273():
                    _st_label_1272 += 1
                else:
                    super()._label('_st_label_1272', block=True)
                    _st_label_1272 -= 1
            (key_C_S, tgs_last_req, tgs_nonce, tgs_key_expiration, tgs_flags, tgs_authtime, tgs_starttime, tgs_endtime, tgs_renew_till, tgs_srealm, tgs_sname, tgs_caddr) = decrypt(tgs_enc_part, key=key_C_TGS)
            if (tgs_nonce == nonce_C_TGS):
                if ((tgs_sname == S) and (tgs_srealm == 'TEST')):
                    self.output('Client - Acquired Ticket from TGS')
            else:
                key_C_S = None
                ticket = None
        if (not (key_C_S == None)):
            ap_authenticator = [5, self._state.realm, self._state.C, None, None, time.time(), None, None, []]
            self.send(('krb_ap_req', self._state.i, 5, 14, dict(), ticket, encrypt(ap_authenticator, key=key_C_S)), to=S)
            self.output('Client - Sent authentication request to server,', S)
            self._state.i += 1

class Node_(da.NodeProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._events.extend([])

    def run(self):
        num_clients = (int(sys.argv[1]) if (len(sys.argv) > 1) else 1)
        num_servers = (int(sys.argv[2]) if (len(sys.argv) > 2) else 1)
        C = self.new(Role_C, num=num_clients)
        S = self.new(Role_AppServer, num=num_servers)
        TGS = self.new(Role_TGS)
        AS = self.new(Role_AuthServer)
        client_keys = dict()
        for client in C:
            key_C_AS = keygen('shared')
            self._setup(client, ('TEST', S, AS, TGS, key_C_AS))
            client_keys[client] = key_C_AS
        as_server_keys = dict()
        tgs_server_keys = dict()
        for server in S:
            key_S_AS = keygen('shared')
            as_server_keys[server] = key_S_AS
            key_S_TGS = keygen('shared')
            tgs_server_keys[server] = key_S_TGS
            self._setup(server, (AS, TGS, key_S_AS, key_S_TGS))
        key_AS_TGS = keygen('shared')
        self._setup(AS, (C, S, TGS, client_keys, as_server_keys, key_AS_TGS))
        self._setup(TGS, (S, AS, key_AS_TGS, tgs_server_keys))
        self._start(AS)
        self._start(TGS)
        self._start(S)
        self._start(C)
